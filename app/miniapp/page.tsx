/**
 * /miniapp — точка входа Telegram Web App.
 * Подгружает SDK, инициализирует и редиректит на главную.
 */

import HomePage from '@/app/page';

export default function MiniAppPage() {
  return <HomePage />;
}
